import * as React from 'react';

// import { Redirect, Route } from 'react-router-dom';



// export interface NAuthRouteProps {
//   component: any;
//   path: string;
//   exact: boolean;
// }

// function NAuthRoute({ component, path, exact }: NAuthRouteProps) {
//   const token = firebase.getUser();

//   return (
//     <>
//       {!token ? (
//         <Route exact={exact} path={path} component={component} />
//       ) : (
//         <Redirect to={'/'} />
//       )}
//     </>
//   );
// }

// export default NAuthRoute;
